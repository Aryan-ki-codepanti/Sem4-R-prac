mean-me<-function(x){
return sum(x)/length(x)
mean-me<-function(x){
sum(x)/length(x)
}
meanme<-function(x){
sum(x)/length(x)
}
x<-1:8
mean-me(x)
meanme(x)
meanme<-function(x){
return sum(x)/length(x)
meanme<-function(x){ # cal mean of list
sum(x)/length(x)
}
sdme<-function(x){  # cal Standard Deviation of list
deviation <- numbers - meanme(numbers)
squared_deviation <- deviation^2
# Find the sum of the squared deviations
sum_squared_deviation <- sum(squared_deviation)
# Find the variance
variance <- sum_squared_deviation / (length(numbers) - 1)
# Find the standard deviation
standard_deviation <- sqrt(variance)
standard_deviation
}
varme<-function(x){  # variance
pow(sdme(x),2)
}
varme(1:10)
varme(1:10)
meanme<-function(x){ # cal mean of list
sum(x)/length(x)
}
sdme<-function(x){  # cal Standard Deviation of list
deviation <- numbers - meanme(numbers)
squared_deviation <- deviation^2
# Find the sum of the squared deviations
sum_squared_deviation <- sum(squared_deviation)
# Find the variance
variance <- sum_squared_deviation / (length(numbers) - 1)
# Find the standard deviation
standard_deviation <- sqrt(variance)
standard_deviation
}
varme<-function(x){  # variance
sdme(x)^2
}
varme(1:10)
meanme<-function(x){ # cal mean of list
sum(x)/length(x)
}
sdme<-function(x){  # cal Standard Deviation of list
deviation <- x - meanme(numbers)
squared_deviation <- deviation^2
# Find the sum of the squared deviations
sum_squared_deviation <- sum(squared_deviation)
# Find the variance
variance <- sum_squared_deviation / (length(numbers) - 1)
# Find the standard deviation
standard_deviation <- sqrt(variance)
standard_deviation
}
varme<-function(x){  # variance
sdme(x)^2
}
varme(1:10)
meanme<-function(x){ # cal mean of list
sum(x)/length(x)
}
sdme<-function(x){  # cal Standard Deviation of list
deviation <- x - meanme(x)
squared_deviation <- deviation^2
# Find the sum of the squared deviations
sum_squared_deviation <- sum(squared_deviation)
# Find the variance
variance <- sum_squared_deviation / (length(numbers) - 1)
# Find the standard deviation
standard_deviation <- sqrt(variance)
standard_deviation
}
varme<-function(x){  # variance
sdme(x)^2
}
varme(1:10)
meanme<-function(x){ # cal mean of list
sum(x)/length(x)
}
sdme<-function(x){  # cal Standard Deviation of list
deviation <- x - meanme(x)
squared_deviation <- deviation^2
# Find the sum of the squared deviations
sum_squared_deviation <- sum(squared_deviation)
# Find the variance
variance <- sum_squared_deviation / (length(x) - 1)
# Find the standard deviation
standard_deviation <- sqrt(variance)
standard_deviation
}
varme<-function(x){  # variance
sdme(x)^2
}
varme(1:10)
sdme(1:10)
meanme<-function(x){ # cal mean of list
sum(x)/length(x)
}
sdme<-function(x){  # cal Standard Deviation of list
deviation <- x - mean(x)
squared_deviation <- deviation^2
# Find the sum of the squared deviations
sum_squared_deviation <- sum(squared_deviation)
# Find the variance
variance <- sum_squared_deviation / (length(x) - 1)
# Find the standard deviation
standard_deviation <- sqrt(variance)
standard_deviation
}
varme<-function(x){  # variance
sd(x)^2
}
meanme<-function(x){ # cal mean of list
sum(x)/length(x)
}
sdme<-function(x){  # cal Standard Deviation of list
deviation <- x - meanme(x)
squared_deviation <- deviation^2
# Find the sum of the squared deviations
sum_squared_deviation <- sum(squared_deviation)
# Find the variance
variance <- sum_squared_deviation / (length(x) - 1)
# Find the standard deviation
standard_deviation <- sqrt(variance)
standard_deviation
}
varme<-function(x){  # variance
sdme(x)^2
}
library(stat.aryan)
library(stat.aryan)
sdme(1:10)
